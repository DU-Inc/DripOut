// Theme typography

export const typography = {
  h1: {
    fontSize: 32,
    fontWeight: 'bold',
    lineHeight: 40,
  },
  h2: {
    fontSize: 28,
    fontWeight: 'bold',
    lineHeight: 36,
  },
  h3: {
    fontSize: 24,
    fontWeight: '600',
    lineHeight: 32,
  },
  body: {
    fontSize: 16,
    fontWeight: '400',
    lineHeight: 24,
  },
  small: {
    fontSize: 12,
    fontWeight: '300',
    lineHeight: 16,
  },
  placeholder: {
    fontSize: 16, // Use theme.typography.fontSize dynamically in components
    fontWeight: '400',
    lineHeight: 24,
    color: '#888888', // Example color for placeholder
  },
  title: {
    fontSize: 24, // Use theme.typography.fontSize dynamically in components
    fontWeight: 'bold',
    lineHeight: 32,
  },
  errorText: {
    fontSize: 14, // Use theme.typography.fontSize dynamically in components
    fontWeight: '400',
    lineHeight: 20,
  },
  buttonText: {
    fontSize: 16, // Use theme.typography.fontSize dynamically in components
    fontWeight: 'bold',
    lineHeight: 24,
  },
  forgotButtonText: {
    fontSize: 16, // Use theme.typography.fontSize dynamically in components
    fontWeight: '400',
    lineHeight: 24,
  },
  defaultFontSize: {
    fontSize: 16, // Default font size to be used globally if no specific size is provided
  }
};
