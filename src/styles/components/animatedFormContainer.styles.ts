import { StyleSheet } from 'react-native';
import { ThemeColors } from '../theme/colors';

export const createAnimatedFormContainerStyles = (theme: ThemeColors) => 
  StyleSheet.create({
    container: {
      width: '100%',
    },
  }); 